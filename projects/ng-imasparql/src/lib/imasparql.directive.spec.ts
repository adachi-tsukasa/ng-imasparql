import { ImasparqlDirective } from './imasparql.directive';

describe('ImasparqlDirective', () => {
  it('should create an instance', () => {
    const directive = new ImasparqlDirective();
    expect(directive).toBeTruthy();
  });
});
